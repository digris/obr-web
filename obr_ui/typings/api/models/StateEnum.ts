/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

/**
 * * `playing` - playing
 * * `stopped` - stopped
 * * `paused` - paused
 * * `buffering` - buffering
 */
export enum StateEnum {
  PLAYING = 'playing',
  STOPPED = 'stopped',
  PAUSED = 'paused',
  BUFFERING = 'buffering',
}
